[package]
name = "xtask"
description = "Build/dev automation for the workspace. Run via `cargo xtask <task>`."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
anyhow.workspace = true
clap.workspace = true
# Only for `corpus::all_roms()`'s static ROM list, so the fetcher and the accuracy suite read one
# source of truth rather than a hand-copied second list that can drift from it.
harness.workspace = true
